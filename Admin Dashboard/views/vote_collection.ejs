<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Candidate Votes</title>
</head>
<body>
  <h1>Candidate Votes</h1>
  <table border="1">
    <thead>
      <tr>
        <% votes.forEach(vote => { %>
          <th><%= vote.candidate.Cname %></th>
        <% }) %>
      </tr>
    </thead>
    <tbody>
      <tr>
        <% votes.forEach(vote => { %>
          <td><%= vote.candidate.partyname %></td> <!-- Replace with actual data to display -->
        <% }) %>
      </tr>
    </tbody>
  </table>
</body>
</html>
