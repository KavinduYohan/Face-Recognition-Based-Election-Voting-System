<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ballot Collection</title>
    <style>
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid black;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
        .container h2 {
            text-align: center;
            margin-bottom: 20px;
            margin-top: 20px; /* Adjust the top margin to push the header down */
            font-weight: bold; /* Make the header bold */
            color: rgba(190, 29, 7, 0.9); /* Light text color */
        }

        .container img {
            display: block;
            margin: 50px auto 40px auto; /* Center image and add bottom margin */
            width: 600px; /* Adjust width as needed */
        }

        .back-button {
            position: absolute;
            top: 20px;
            right: 20px;
            background-color: rgba(190, 29, 7, 0.9);
            color: #fff;
            border-radius: 8px;
            padding: 10px 20px;
            text-decoration: none;
            transition: transform 0.2s;
        }

        .back-button:hover {
            background-color: rgba(199, 173, 56, 0.9);
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <div class="container">
        <a href="javascript:history.back()" class="back-button">Back</a>
        <img src="https://elections.gov.lk/images/logo-01.png" alt="Emblem of Sri Lanka">
        <h2>Parliament Election</h2>
    </div>
    <h1>Ballot Collection</h1>
    <table>
        <thead>
            <tr>
                <th>Ballot ID</th>
                <th>Party</th>
                <th>Candidate NIC 1</th>
                <th>Candidate NIC 2</th>
                <th>Candidate NIC 3</th>
                <th>Date</th>
                <th>Time</th>
            </tr>
        </thead>
        <tbody>
            <% if (ballotsp && ballotsp.length > 0) { %>
                <% ballotsp.forEach(ballotp => { %>
                    <tr>
                        <td><%= ballotp._id %></td>
                        <td><%= ballotp.party %></td>
                        <td><%= ballotp.candidate_nic1 %></td>
                        <td><%= ballotp.candidate_nic2 %></td>
                        <td><%= ballotp.candidate_nic3 %></td>
                        <td><%= new Date(ballotp.created_at).toISOString().split('T')[0] %></td>
                        <td><%= new Date(ballotp.created_at).toTimeString().split(' ')[0] %></td>
                    </tr>
                <% }); %>
            <% } else { %>
                <tr>
                    <td colspan="7">No ballots found</td>
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>
